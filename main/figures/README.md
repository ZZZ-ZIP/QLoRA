# Figures

This directory is reserved for small public figures used in documentation.

Generated plots are ignored by default. Common local output folders include:

- `outputs/figures/`
- `results_paper_extension/*/rank_sweep/`

Only manually curated figures should be copied here for release.
