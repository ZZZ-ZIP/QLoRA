# Results

This directory is reserved for lightweight, public result summaries.

Generated metrics, prediction files, checkpoints, adapter weights, and large experiment logs are ignored by default. Local experiment scripts usually write to:

- `outputs/`
- `results_paper_extension/`
- `runs/`

These folders are excluded from Git to avoid uploading private data, model weights, and large artifacts.
