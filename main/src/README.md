# Source Package

Reusable package modules can be placed here if the project is later refactored into an installable Python package.

The current experiment scripts keep their original layout under `scripts/` so that existing commands remain compatible.
